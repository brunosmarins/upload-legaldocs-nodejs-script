# this project add legal documents on cropwise system

# for use you need:

```bash
npm ci

```

```bash
npx run start -t "<token>" -u "url of service"

```

url example:
"https://api.staging.base.cropwise.com/v2/legal-docs"

You need get the token using the cropwise api to it.
